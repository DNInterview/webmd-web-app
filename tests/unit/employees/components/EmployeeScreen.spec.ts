import EmployeeScreen from "@/modules/employees/components/EmployeeScreen.vue";

describe("EmployeeScreen", () => {
  describe("methods", () => {
    describe("getEmployees", () => {
      it("sets the employees state as the result from the server", () => {
        expect(true).toBeTruthy();
      });
    });
  });
});
